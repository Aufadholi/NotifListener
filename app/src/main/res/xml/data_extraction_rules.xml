<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for WaNotification.
    Excludes sensitive user data from cloud backup and device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude contact filter preferences (sensitive data) -->
        <exclude domain="sharedpref" path="contact_filter_prefs.xml"/>
        <!-- Exclude TTS settings -->
        <exclude domain="sharedpref" path="tts_prefs.xml"/>
        <!-- Exclude database and cache -->
        <exclude domain="database"/>
        <exclude domain="cache"/>
    </cloud-backup>
    <device-transfer>
        <!-- Exclude contact filter preferences from device-to-device transfer -->
        <exclude domain="sharedpref" path="contact_filter_prefs.xml"/>
    </device-transfer>
</data-extraction-rules>

